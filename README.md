# jalbertoferreira.com

Personal academic website for José Alberto Ferreira — plain HTML/CSS, hosted on GitHub Pages.

## Structure
- `index.html` — Home
- `cv.html` — CV (HTML + link to `assets/CV_AlbertoFerreira.pdf`)
- `research.html` — Research
- `compnet.html` — CompNet
- `portugal.html` — Portugal
- `css/style.css` — single stylesheet (edit variables at top to restyle)
- `assets/` — optimized photo, CV PDF, favicons
- `CNAME`, `sitemap.xml`, `robots.txt` — hosting + SEO config

## Editing
Plain HTML — open a file, edit text between the tags, save, commit, push. Pages go live in ~1 min.
Search for `[Placeholder` to find content still to be written.

## Deploy
Pushing to the `main` branch auto-deploys via GitHub Pages (Settings → Pages → Source: main).
